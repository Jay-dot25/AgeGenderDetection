# Install in addition to requirements.txt if you want to retrain the model
# pip install -r requirements.txt -r requirements-train.txt

pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
